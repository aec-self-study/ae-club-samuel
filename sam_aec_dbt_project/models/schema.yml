
version: 2

models:
  - name: customers
    description: "One row per customer showing first order date and number of total orders"
    columns:
      - name: customer_id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null
